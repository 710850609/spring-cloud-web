server:
  port: 10000
  ssl:
    enabled: true
    key-alias: webServer
    key-store: classpath:webServer.p12
    key-store-type: PKCS12
    key-store-password: 123456

spring:
  application:
    name: web-api-provider
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
#        file-extension: yaml