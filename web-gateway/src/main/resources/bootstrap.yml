server:
  port: 8080

spring:
#  application:
#    name: '@pom.artifactId@'
#  profiles:
#    active: '@profile.active.id@'
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        exclude: '*'